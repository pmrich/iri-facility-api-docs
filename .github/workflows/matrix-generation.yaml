name: IRI Endpoints Validation Matrix generation

on:
  workflow_dispatch:
    inputs:
      site:
        description: "Select site to validate"
        type: choice
        required: true
        default: all
        options:
          - all
          - localhost
          - ESnet_East
          - ESnet_West
          - NERSC
          - ALCF
          - OLCF_Open
          - OLCF_Moderate
          - SLAC

permissions:
  contents: write

env:
  OFFICIAL_SCHEMA: specification-v1/openapi/all_spec_v1.yaml
  IRI_API_TOKEN: "12345"

jobs:

  validate:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: localhost
            site: http://127.0.0.1:8000
            type: local
          - name: ESnet_East
            site: https://iri-dev.ppg.es.net/
            type: remote
          - name: ESnet_West
            site: https://esnet-west.sdn-sense.net/
            type: remote
          - name: NERSC
            site: https://api.iri.nersc.gov/
            type: remote
          - name: ALCF
            site: https://api.alcf.anl.gov/
            type: remote
          - name: OLCF_Open
            site: https://amsc-open.s3m.olcf.ornl.gov/
            type: remote
          - name: OLCF_Moderate
            site: https://amsc-moderate.s3m.olcf.ornl.gov/
            type: remote
          - name: SLAC
            site: https://iri.slac.stanford.edu/
            type: remote

    steps:
      - name: Set flag for selected site run
        id: siterun
        run: |
          if [[ "${{ github.event.inputs.site }}" == "all" || \
                "${{ matrix.name }}" == "${{ github.event.inputs.site }}" ]]; then
            echo "run=true" >> $GITHUB_OUTPUT
          else
            echo "run=false" >> $GITHUB_OUTPUT
            echo "Skipping ${{ matrix.name }}"
          fi

      - name: Checkout docs repository
        if: steps.siterun.outputs.run == 'true'
        uses: actions/checkout@v4

      - name: Checkout Python API repository
        if: steps.siterun.outputs.run == 'true'
        uses: actions/checkout@v4
        with:
          repository: doe-iri/iri-facility-api-python
          path: api

      - uses: actions/setup-python@v5
        if: steps.siterun.outputs.run == 'true'
        with:
          python-version: "3.14"

      - name: Install verification dependencies
        if: steps.siterun.outputs.run == 'true'
        run: pip install -r verification/requirements.txt

      # ---------- LOCAL API ----------
      - name: Build Python API Docker image
        if: matrix.type == 'local' && steps.siterun.outputs.run == 'true'
        run: docker build -t iri-facility-api-base ./api

      - name: Run Python API container
        if: matrix.type == 'local' && steps.siterun.outputs.run == 'true'
        run: |
          docker run -d -p 8000:8000 --name iri-facility-api-base \
            -e IRI_API_ADAPTER_facility=app.demo_adapter.DemoAdapter \
            -e IRI_API_ADAPTER_status=app.demo_adapter.DemoAdapter \
            -e IRI_API_ADAPTER_account=app.demo_adapter.DemoAdapter \
            -e IRI_API_ADAPTER_compute=app.demo_adapter.DemoAdapter \
            -e IRI_API_ADAPTER_filesystem=app.demo_adapter.DemoAdapter \
            -e IRI_API_ADAPTER_task=app.demo_adapter.DemoAdapter \
            -e API_URL_ROOT=http://127.0.0.1:8000 \
            -e IRI_API_TOKEN=${IRI_API_TOKEN} \
            iri-facility-api-base

      - name: Wait for API readiness
        if: matrix.type == 'local' && steps.siterun.outputs.run == 'true'
        run: |
          for i in {1..60}; do
            if curl -fs http://127.0.0.1:8000/openapi.json; then exit 0; fi
            sleep 2
          done
          docker logs iri-facility-api-base
          exit 1

      # ---------- VALIDATION ----------
      - name: Run site validation
        if: steps.siterun.outputs.run == 'true'
        run: |
          mkdir -p verification/output
          python verification/site-validation.py \
            --official-schema "$OFFICIAL_SCHEMA" \
            --sites "${{ matrix.site }}"

      - name: Upload validation artifacts
        if: steps.siterun.outputs.run == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: validation-${{ matrix.name }}
          path: verification/output/

      # Stop local container
      - name: Stop and remove local container
        if: always() && matrix.type == 'local' && steps.siterun.outputs.run == 'true'
        run: docker stop iri-facility-api-base || true && docker rm iri-facility-api-base || true

# Build matrix report after all validations are complete
  build-global-report:
    needs: validate
    runs-on: ubuntu-latest

    steps:

      - name: Set report date (global var)
        id: reportdate
        run: echo "DATE=$(date -u +%Y-%m-%d)" >> $GITHUB_OUTPUT

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install verification dependencies
        run: pip install -r verification/requirements.txt

      - name: Download all artifacts
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          path: collected-output

      - name: Merge outputs into single directory
        run: |
          mkdir -p verification/test_results/latest
          shopt -s nullglob

          for artifact in collected-output/*; do
            for site in "$artifact"/*; do
              host=$(basename "$site")

              echo "Updating latest results for $host"

              rm -rf verification/test_results/latest/$host
              mkdir -p verification/test_results/latest/$host

              cp -r "$site"/* verification/test_results/latest/$host/
            done
          done

      - name: Prepare output from latest data
        run: |
          rm -rf verification/output
          mkdir -p verification/output

          shopt -s nullglob
          for site in verification/test_results/latest/*; do
            cp -r "$site" verification/output/
          done

      - name: Build matrix report
        run: |
          python verification/build-report.py \
            --official-schema "$OFFICIAL_SCHEMA"

      - name: Store versioned + latest matrix report + per-site results
        run: |
          DATE=${{ steps.reportdate.outputs.DATE }}
          mkdir -p verification/test_results/history/$DATE

          if [ -f verification/output/full-report.md ]; then
            cp verification/output/full-report.md \
              verification/test_results/history/$DATE/
            cp verification/output/full-report.md \
              verification/test_results/latest/full-report.md
          fi

          for site in verification/output/*; do
            [ -d "$site" ] || continue

            host=$(basename "$site")
            mkdir -p verification/test_results/history/$DATE/$host
            cp -r "$site"/* verification/test_results/history/$DATE/$host/
          done

      - name: Commit matrix report
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          git add verification/test_results/
          git commit -m "Matrix report generated at ${{ steps.reportdate.outputs.DATE }}" || echo "No changes"

          git push

